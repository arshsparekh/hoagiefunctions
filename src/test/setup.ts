// Vitest global setup: register jest-dom matchers (toBeInTheDocument, etc.).
// React Testing Library auto-cleans between tests because vitest globals are on.
import '@testing-library/jest-dom/vitest'
